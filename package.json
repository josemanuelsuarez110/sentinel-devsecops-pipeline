{
  "name": "sentinel-devsecops-pipeline",
  "version": "1.0.0",
  "description": "A secure Node.js application for DevOps pipeline demonstration.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 0",
    "security-audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
